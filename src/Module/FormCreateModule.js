

/*

import React, { useState } from 'react'
import ChipInput from 'material-ui-chip-input'

const FormCreateModule = () => {

    const [ state, setState ] = useState({ Modulo: { Sistema: undefined, Module: [ ] } })

return <></>

// <ChipInput value={yourChips} onAdd={(chip) => handleAddChip(chip)} onDelete={(chip, index) => handleDeleteChip(chip, index)}/>

}

Centro Sistema Modulos Sub Modulos Proceso

https://github.com/TeamWertarbyte/material-ui-chip-input

*/

